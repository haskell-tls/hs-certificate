# ~*~ auto-generated by haskell-ci with config : aa407cb8ef21ea4f031224d579f94e1283708b31d6ac399b28a0fe0224a0c5dc ~*~

# Use new container infrastructure to enable caching
sudo: false

# Caching so the next build will be fast too.
cache:
  directories:
  - $HOME/.ghc
  - $HOME/.stack
  - $HOME/.local

matrix:
  include:
  - { env: BUILD=stack RESOLVER=ghc-8.0, compiler: ghc-8.0, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.2, compiler: ghc-8.2, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.4, compiler: ghc-8.4, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.6, compiler: ghc-8.6, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.6, compiler: ghc-8.6, language: generic, addons: { apt: { packages: [ libgmp-dev ] } }, os: osx }
  - { env: BUILD=stack RESOLVER=ghc-8.8, compiler: ghc-8.8, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=hlint, compiler: hlint, language: generic }
  - { env: BUILD=weeder, compiler: weeder, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }
  allow_failures:
  - { env: BUILD=hlint, compiler: hlint, language: generic }
  - { env: BUILD=weeder, compiler: weeder, language: generic, addons: { apt: { packages: [ libgmp-dev ] } } }

install:
  - export PATH=$HOME/.local/bin::$HOME/.cabal/bin:$PATH
  - mkdir -p ~/.local/bin
  - |
    case "$BUILD" in
      stack|weeder)
        if [ `uname` = "Darwin" ]
        then
          travis_retry curl --insecure -L https://www.stackage.org/stack/osx-x86_64 | tar xz --strip-components=1 --include '*/stack' -C ~/.local/bin
        else
          travis_retry curl -L https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
        fi
      ;;
    cabal)
      ;;
    esac

script:
- |
  set -ex
  if [ "x${RUNTEST}" = "xfalse" ]; then exit 0; fi
  case "$BUILD" in
    stack)
      # create the build stack.yaml
      case "$RESOLVER" in
      ghc-8.0)
        echo "{ resolver: lts-9.21, packages: [ x509/, x509-store/, x509-system/, x509-validation/, x509-util/ ], extra-deps: [ cryptonite-0.25, basement-0.0.6, foundation-0.0.19, memory-0.14.14 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.2)
        echo "{ resolver: lts-11.22, packages: [ x509/, x509-store/, x509-system/, x509-validation/, x509-util/ ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.4)
        echo "{ resolver: lts-12.9, packages: [ x509/, x509-store/, x509-system/, x509-validation/, x509-util/ ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --no-test --no-bench --haddock --no-haddock-deps
        ;;
      ghc-8.6)
        echo "{ resolver: lts-13.26, packages: [ x509/, x509-store/, x509-system/, x509-validation/, x509-util/ ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.8)
        echo "{ resolver: nightly-2019-09-29, packages: [ x509/, x509-store/, x509-system/, x509-validation/, x509-util/ ], extra-deps: [ \"asn1-encoding-0.9.6@sha256:784e936495b1408a7831ff000bef25993b910ca9f008f69e1590da80a9d40f7b,1988\", \"asn1-parse-0.9.5@sha256:77c0126d63070df2d82cb4cfa4febb26c4e280f6d854bc778c2fa4d80ce692b8,976\" ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      esac
      ;;
    hlint)
      curl -sL https://raw.github.com/ndmitchell/hlint/master/misc/travis.sh | sh -s . --cpp-define=__GLASGOW_HASKELL__=800 --cpp-define=x86_64_HOST_ARCH=1 --cpp-define=mingw32_HOST_OS=1
      ;;
    weeder)
      stack --no-terminal build --install-ghc
      curl -sL https://raw.github.com/ndmitchell/weeder/master/misc/travis.sh | sh -s .
      ;;
  esac
  set +ex


